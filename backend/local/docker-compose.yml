services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: daltime-dynamodb
    ports:
      - "8000:8000"
    volumes:
      - ./data/dynamodb:/home/dynamodblocal/data
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath /home/dynamodblocal/data"
    restart: unless-stopped
